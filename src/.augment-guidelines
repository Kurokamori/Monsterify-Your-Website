## Windows Development Environment

- Operating System: Windows 11
- Terminal: PowerShell
- Use semicolon (;) instead of && for command chaining
- Example: `pnpm build ; pnpm start` instead of `pnpm build && pnpm start`